#





def list_ifname_ip(filename):
    
    file = open(filename)
    dic = dict()
    for line in file:
        line = line.split()
        if any(line) == 'nameif'
            word = word.strip()
        if  
        dic[nameif]=(IPaddress,NetMask)
return dic







list_ifname_ip('running-config.cfg')


